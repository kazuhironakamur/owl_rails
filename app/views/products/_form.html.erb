<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code, id: :product_code %>
  </div>

  <div class="field">
    <%= form.label :catchcopy %>
    <%= form.text_field :catchcopy, id: :product_catchcopy %>
  </div>

  <div class="field">
    <%= form.label :weight_g %>
    <%= form.number_field :weight_g, id: :product_weight_g %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
